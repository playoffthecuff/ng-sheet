import { InvertHighlightDirective } from './invert-highlight-directive';

describe('InvertHighlightDirective', () => {
	it('should create an instance', () => {
		const directive = new InvertHighlightDirective();
		expect(directive).toBeTruthy();
	});
});
