import { ProgressToColorPipe } from './progress-to-color-pipe';

describe('ProgressToColorPipe', () => {
	it('create an instance', () => {
		const pipe = new ProgressToColorPipe();
		expect(pipe).toBeTruthy();
	});
});
